import axios from "axios";
import { url } from "../../url";
import { Login } from "@mui/icons-material";
import moment from "moment";

export const utils = {
    convertCityName: () => {
        
    }
};
